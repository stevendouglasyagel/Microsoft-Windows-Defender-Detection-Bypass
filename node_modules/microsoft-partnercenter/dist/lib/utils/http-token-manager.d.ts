import { AxiosRequestConfig } from 'axios';
import { IPartnerCenterConfig } from '../types/common.types';
export declare class TokenManager {
    private config;
    private accessToken;
    private _refreshToken;
    private reAuthed;
    private retry;
    constructor(config: IPartnerCenterConfig);
    getInitializedRefreshToken(): Promise<string | null>;
    getAccessToken(): Promise<string>;
    handleAuthenticationError(err: any, requestConfig: AxiosRequestConfig): Promise<import("axios").AxiosResponse<any, any>>;
    private authenticate;
    private prepareAuthData;
    private isTokenExpired;
}
export declare function initializeHttpAndTokenManager(config: IPartnerCenterConfig): {
    agent: import("axios").AxiosInstance;
    tokenManager: TokenManager;
};
